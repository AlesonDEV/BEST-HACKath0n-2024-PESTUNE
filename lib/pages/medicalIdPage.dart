import 'package:flutter/material.dart';

class MedicalIdPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Медичний ID'),
      ),
      body: Center(
        child: Text('Медичний ID сторінка'),
      ),
    );
  }
}
